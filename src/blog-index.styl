:local(.blog-index) {
  // background: #4390B2;
  // background: #FFFA85;
  // padding: 5em 1.5em 1em;

  a {
    color: black;
    text-decoration: none;
  }
}

:local(.blog-index-entry) {
  margin: 2em 0;
  padding: 1em 1.5em 1em;
  background: #FFFA85;

  h1, h3 {
    margin-bottom: 0;
  }

  a:nth-child(1) & {
    margin: 0;
  }
}
